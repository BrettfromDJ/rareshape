# Development only. Next loads this for `pnpm dev` and never for a production
# build, so the harness tools give a fresh checkout something to look at while
# the index has no real tools on it yet.
NEXT_PUBLIC_SHOW_HARNESS=1
